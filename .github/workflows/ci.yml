on: [push]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Run linter
      shell: 'script -q -e -c "bash {0}"'
      run: make lint

    - name: Build krakend executable
      shell: 'script -q -e -c "bash {0}"'
      run: make test
